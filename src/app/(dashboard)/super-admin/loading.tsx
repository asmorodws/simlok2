/**
 * Loading page for Super Admin dashboard
 */

import PageLoader from '@/components/ui/loading/PageLoader';

export default function Loading() {
  return <PageLoader message="Memuat dashboard super admin..." />;
}
