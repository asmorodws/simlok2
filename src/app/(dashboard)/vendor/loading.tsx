/**
 * Loading page for Vendor dashboard
 */

import PageLoader from '@/components/ui/loading/PageLoader';

export default function Loading() {
  return <PageLoader message="Memuat dashboard vendor..." />;
}
